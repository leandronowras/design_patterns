import SystemUserProxy from "./system-user/SystemUserProxy"

// test("should create an user with delay", async () => {
//     const user = new SystemUserProxy("Leandro", "leandronowras")
//     const userAddressCache = await user.getAddresses()

//     expect(userAddressCache).toStrictEqual([{"number": 3, "street": "Aguas Claras"}])
// })